# Dev: de trong — Vite proxy /api -> localhost:8080
# VITE_API_URL=

# Production (Vercel): URL day du toi backend, co /api
# Vi du Cloudflare Tunnel / reverse proxy HTTPS:
# VITE_API_URL=https://hrm-api.minhan.local/api
