package com.minhan.hrm.entity;

public enum EmployeeStatus {
    /** Nhân viên chính thức. */
    ACTIVE,
    /** Đang thử việc (1–3 tháng). */
    PROBATION,
    /** Thực tập. */
    INTERN,
    ON_LEAVE,
    /** Đã nghỉ việc. */
    TERMINATED
}
