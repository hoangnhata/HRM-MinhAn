package com.minhan.hrm.entity;

/**
 * Loại hợp đồng làm việc theo cột «Loại HĐ» trong file nhân lực.
 * TTG = toàn thời gian, BTG = bán thời gian.
 */
public enum EmploymentType {
    FULL_TIME,
    PART_TIME
}
